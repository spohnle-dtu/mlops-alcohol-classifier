name: Docker Build

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "dockerfiles/**"
      - "src/**"
      - "requirements_docker.txt"
      - "pyproject.toml"
      - ".github/workflows/**" # Added so workflow changes trigger a build
  push:
    branches: ["main"]
    paths:
      - "dockerfiles/**"
      - "src/**"
      - "requirements_docker.txt"
      - "pyproject.toml"
      - ".github/workflows/**" # Added so workflow changes trigger a build

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - name: frontend
            dockerfile: dockerfiles/frontend.Dockerfile
            tag: alcohol-frontend:ci
          - name: api
            dockerfile: dockerfiles/api.Dockerfile
            tag: alcohol-api:ci

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install DVC
        uses: iterative/setup-dvc@v1

      - name: Pull data from DVC
        run: |
          # Force local config to expect a service account on the runner
          dvc remote modify storage --local gdrive_use_service_account true
          dvc remote modify storage --local gdrive_service_account_json_file_path /tmp/credentials.json
          dvc pull --force
        env:
          GDRIVE_CREDENTIALS_DATA: ${{ secrets.GDRIVE_CREDENTIALS_DATA }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build ${{ matrix.target.name }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.target.dockerfile }}
          push: false
          tags: ${{ matrix.target.tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
